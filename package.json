{
  "name": "xanh-arcade",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check games.js && node --check game-rules.js && node --check battle-engine.js && node --check battle.js && node --check beta-shop.js"
  },
  "engines": {
    "node": ">=22"
  }
}
